import styled from "styled-components";

export const Input = styled.input`
  margin: 0;
  width: 100%;
  display: block;
  font-family: inherit;
  font-size: inherit;
  line-height: inherit;
  outline-color: blue;
`;

export const Label = styled.p`
  font-size: 16px;
  margin: 10px 0;
`;
